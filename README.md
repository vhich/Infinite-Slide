# Infinite slide

This is a dynamic infinit slide, developed using vaniila javascript.
To add a new slide, just go to the _slideData.js_ file, create a new object for the new slide e.g ```{backgrounColour: red}``` 
and save, the slide will automatically be added to the *DOM.*
